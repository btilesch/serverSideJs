<div class="flex space-x-4 items-center w-full">
    <label class="w-1/4 text-left block mb-2 text-md font-medium dark:text-gray-300" for="recipe-name">
        Recipe name
    </label>
    <input
            class="w-3/4 rounded-lg dark:bg-gray-300 focus:border-indigo-700 focus:ring-indigo-700"
            id="recipe-name"
            name="name"
            placeholder="Recipe name"
            required
            type="text"
            <%= disabled === true ? 'disabled' : '' %>
            value="<%= (typeof line === 'undefined') ? '' : line.name %>"/>
</div>
<div class="flex space-x-4 items-center w-full">
    <label class="w-1/4 text-left block mb-2 text-md font-medium dark:text-gray-300" for="recipe-ma01"> MA01 </label>
    <select
            id="recipe-ma01"
            name="_ma01"
            class="w-3/4 rounded-lg dark:bg-gray-300 focus:border-indigo-700 focus:ring-indigo-700"
            required
            <%= disabled === true ? 'disabled' : '' %>>
        <option value="" <%= (typeof ma01Id !== 'undefined') ? '' : 'selected' %> disabled>Select</option>
        <% ma01List.forEach(ma01Recipe => { %>
            <option
                    value="<%= ma01Recipe._id %>"
                    <%= (typeof ma01Id !== 'undefined') && (ma01Recipe._id.toString() === ma01Id.toString()) ? 'selected' : '' %> >
                <%= ma01Recipe.name %>
            </option>
        <% }); %>
    </select>
</div>
<div class="flex space-x-4 items-center w-full">
    <label class="w-1/4 text-left block mb-2 text-md font-medium dark:text-gray-300" for="recipe-ma02">
        MA02
    </label>
    <select
            id="recipe-ma02"
            name="_ma02"
            class="w-3/4 rounded-lg dark:bg-gray-300 focus:border-indigo-700 focus:ring-indigo-700"
            required
            <%= disabled === true ? 'disabled' : '' %>>
        <option value="" <%= (typeof ma02Id !== 'undefined') ? '' : 'selected' %> disabled>Select</option>
        <% ma02List.forEach(ma02Recipe => { %>
            <option
                    value="<%= ma02Recipe._id %>"
                    <%= (typeof ma02Id !== 'undefined') && (ma02Recipe._id.toString() === ma02Id.toString()) ? 'selected' : '' %>>
                <%= ma02Recipe.name %></option>
        <% }); %>
    </select>
</div>